{
  "version": 2,
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1.js" },
    { "src": "/XO-Fizzx", "dest": "/admin.html" },

    { "src": "/", "dest": "/index.html" },   // fallback eksplisit

    { "handle": "filesystem" }
  ]
}
